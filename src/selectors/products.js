/**
 * Select product
 * @param {*} state
 */
export const selectProduct = (state) => state.products.product;

/**
 * Select my product
 * @param {*} state
 */
export const selectMyProduct = (state) => state.products.myProduct;
